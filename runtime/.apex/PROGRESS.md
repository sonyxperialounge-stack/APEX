# Progress

## Completed

## Blocked
- (none)

## RESUME POINT
Next action: Live checklist for Claude Code L1, Cursor L1 and macOS/Linux attach — none of those hosts is installed on this machine, so CI covers the platforms and the host bindings stay unverified live.
Do not redo: REQ-001..REQ-011
Verify first: npm run verify
Watch out: safe() must re-throw BlockedError; never trust a host path; attach must not touch unnamed hosts; tests assert the SPEC.
