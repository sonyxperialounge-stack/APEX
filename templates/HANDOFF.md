# Handoff — <timestamp>

## Objective
<what is being built, and where>

## Sources of truth
<plan files> · <reference docs>
User restrictions: never read <...>; never modify <...>

## State
<n>/<total> requirements verified. Table: REQUIREMENTS.md

## Architecture you need to know
<the two or three facts that are not obvious from any single file>

## Commands
test: `` · types: `` · lint: `` · build: `` · run: ``

## Environment
<runtime version, venv, ports, env vars, what is NOT available here>

## Known failures
<REQ-ID> blocked on <...> (see V-nnn)

## Subagents
<any in-flight or failed work, and what already survives — do not restart from zero>

## RESUME
Start from:
Do not redo:
Verify first:

<!-- The test: could a model with ZERO context continue from this file plus the plan,
     losing nothing but time? If not, it is incomplete.
     Most-regretted omissions: environment facts, the trap you just learned, what NOT to
     redo, and WHY something is the way it is (so nobody "fixes" it back). -->
