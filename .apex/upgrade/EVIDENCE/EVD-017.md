# EVD-017
Packet: WP-023 (normalization, dedupe, near-duplicate detection)
When: 2026-09-10
Command: npm run verify
Cwd: D:/APEX/Army-V3/runtime
Exit: 0

Focused run: 13 tests / 2 suites, 0 fail.

Full pipeline (literal tail):

```text
ℹ tests 821
ℹ suites 184
ℹ pass 821
ℹ fail 0
ℹ cancelled 0
ℹ skipped 0
ℹ todo 0
ℹ duration_ms 18280.8324
```

Done-when — 11 §13 fixture set classifies correctly:
- exact duplicate (normalized identical, incl. punctuation/case/zero-width) -> equivalent ✔
- punctuation/case duplicate -> equivalent via normalization ✔
- "npm" -> "pnpm" -> conflict_candidate (distinct value for one subject, NOT a merge) ✔
- two unrelated facts with high word overlap -> different semantic keys never auto-merge
  (isSameSubject false); within one key the score lands in the review zone, 0.75..0.92 ✔
- same fact from two models with different provenance -> mergeExactDuplicate refreshes
  provenance only; text never rewritten; revision bumps ✔
- Devanagari correction/variants: nukta variants (यूज़र/यूजर) fold to equivalent ✔
- multilingual corpus safety: Devanagari words survive normalization intact ✔

Real defects found and fixed in-packet (all probe-verified):
1. The letter class lacked \p{M} — Devanagari vowel signs (Mn/Mc category, not L) were
   being stripped, splitting every Indic word mid-syllable. Now included.
2. Trailing "." survived the allowed-class (npm. != npm) — trailing separators fold.
3. Nukta (U+093C) is a spelling variant, not a different word — folded during
   normalization so Hinglish/Devanagari variants dedupe through the TOKEN path (better
   than relying on the 3-gram rescue).
4. The 3-gram fallback's *0.95 damping defeated its purpose (11 §11 exists precisely
   for weak-token scripts); damping removed — trigram stands on its own and can only
   raise a classification, never lower it. Merging is still impossible across semantic
   keys regardless of score.
