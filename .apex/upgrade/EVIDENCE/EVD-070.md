# EVD-070 — WP-070 START-HERE V2 (48; Phase-7A packet)

Date: 2026-09-11 · Packet: WP-070 (Dep WP-068) · Branch: upgrade/army-v4

## Context

48 is an edit, not a rewrite: keep the seven-step loop, compressed core,
three levels, autonomy table and degraded section; fold 06's B0–B11 into
the existing steps; add the durable-state map, the readiness contract and
core item 8. Done-when: SH-T01..T07 pass, including the token ceiling.

## What was built

- `~START-HERE.md` + `~runtime/payload/START-HERE.md` (sync):
  - Step 5 gains the global-home resolution — the three modes
    (`READ_WRITE` / `READ_ONLY` / `VOLATILE`, verified against
    `src/core/paths.ts` `HomeMode`) in three sentences with a pointer to
    `core/14-DURABLE-STATE.md` (48 §11).
  - Step 7 gains the readiness contract verbatim from 48 §3 — five lines,
    the language rule (`Kaam bataiye.` / `Ready — what should I work on?`),
    the omission rule and the honest-unavailable clause.
  - `## WHAT SURVIVES WHAT` inserted after the three levels (48 §9), with
    the archive row naming `<global home>/archive/` per the WP-068
    code-wins rule, and a durable-state-survives-levels line.
  - Compressed core item 8 "Remembered is not proven." verbatim (48 §10).
  - The degraded section disclaims persistence (48 §11): "In this mode
    nothing persists between sessions. Say so before the user relies on it."
  - "For the human" gains the runtime commands (doctor, memory, skills,
    archive, status/gate) — each name checked against `src/cli/index.ts`
    and its sub-CLIs (`memory list|inspect|add|correct|retract|export`,
    `skills search|view|stage|promote|retire`,
    `archive discover|browse|read|scroll`); the 48-era draft list named
    memory subcommands that do not exist, so the file lists the real ones.
- `~runtime/src/engines/cortex.ts` — SH-T04 defect fix: project memory was
  injected as a bare `PROJECT MEMORY` block while 48 §4 / 30 §3 require
  every retrieved-memory block inside the data wrapper. It now renders as
  `<APEX_DATA source="project-memory" …>`; the dead `memorySection` was
  removed. (Packet scope note: a one-line render fix that the SH-T04
  acceptance for this packet directly depends on; no engine contract
  changed.)
- `+runtime/test/cli/starthere.test.ts` — SH-T01 (ceiling ≤ 6000 estimated
  tokens, both copies), SH-T02 (every routed file exists in repo root and
  payload, install/build being repository-only), SH-T03 (readiness
  contract verbatim + omission/language rules), SH-T05 (policy-shaped
  memory denied at ingestion; multilingual text stays allow), SH-T06
  (staged is never called saved — doctrine rule + CLI wording), SH-T07
  (degraded section disclaims persistence), plus the WHAT-SURVIVES-WHAT
  and core-item-8 presence checks. Transcript-level SH-T03/SHT06 live
  wording stays with the L0 conformance suite (WP-085).
- `~runtime/test/engines/cortex.test.ts` — SH-T04: project memory renders
  inside the wrapper with the three fixed sentences.

## Verify

`npm run verify` → 1327 pass, 0 fail, 0 cancelled, exit 0 (~42s)
(was 1313; +14 new: 11 in starthere.test.ts, 1 SH-T04 in cortex.test.ts,
plus the promoted assertions inside existing blocks).

Token ceiling measured: root START-HERE.md ≈ 16.5 KB ≈ 4,133 estimated
tokens (ceiling 6,000) — asserted in CI by SH-T01.

## Acceptance mapping

| Requirement | How |
|---|---|
| SH-T01 | starthere.test.ts ceiling test, both copies |
| SH-T02 | starthere.test.ts routed-files test (repo + payload) |
| SH-T03 | starthere.test.ts canonical-text test; live transcript at WP-085 |
| SH-T04 | cortex.test.ts wrapper test (project memory now wrapped — fixed this packet) |
| SH-T05 | starthere.test.ts ingestion-deny test on a real injection phrase |
| SH-T06 | starthere.test.ts doctrine + CLI wording tests; live transcript at WP-085 |
| SH-T07 | starthere.test.ts degraded-disclaimer test |

## Surprises

- The 48 §3 readiness draft's `Unavailable:` line and the §9 map were
  straight ports. The one mismatch was the "For the human" command list:
  the CLI exposes `memory add|inspect|correct|retract`, not
  `search|stage|commit` (those are MCP-tool names), so the file lists the
  commands that actually exist.
- SH-T04 uncovered a real gap: the project-memory block predated the
  wrapper work and had never been wrapped. Fixed in this packet because
  the acceptance criterion names it.

## Files

- `~START-HERE.md`, `~runtime/payload/START-HERE.md` (sync)
- `~runtime/src/engines/cortex.ts`
- `+runtime/test/cli/starthere.test.ts`
- `~runtime/test/engines/cortex.test.ts`
- `~.apex/upgrade/STATE.json` (WP-070 IN_PROGRESS → DONE)

## Next

WP-071 — adapter refresh (06 §2 B3).
