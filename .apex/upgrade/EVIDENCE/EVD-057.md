# EVD-057 — WP-057 Capability doctrine (48 §2, §5)

**Date:** 2026-09-11
**Branch:** `upgrade/army-v4`
**Dependencies:** WP-055 (capability-loss recovery, which this doctrine names), doc 21 (registry + effects), doc 48 §2/§5 (routing + canonical unavailability wording)
**Verify:** `npm run verify` → 1229 pass, 0 fail, exit 0 (~40s); payload link-integrity suite runs inside it (7/7)

## Context

- **48 §2** — routing table for START-HERE step 6 gains `core/16-CAPABILITIES.md` → "When a needed tool seems missing, or before planning tool-heavy work". Doctrine files must reach `runtime/payload/core/` via `scripts/sync-payload.mjs` or packaging tests fail (42 §11).
- **48 §5** — canonical honest-unavailability statement, required verbatim-shape in the doctrine: `[capability] is UNAVAILABLE in this host.` / `Effect on the task: …` / `Fallback: …`; forbidden are pretending to call the tool, describing a hypothetical result as if observed, silently substituting without saying so.
- **21** — capability registry semantics the doctrine must state: canonical vendor-neutral IDs, ONE effects taxonomy (`READ`…`EXTERNAL_SIDE_EFFECT`), conservative inference (unknown lands on `EXTERNAL_SIDE_EFFECT`), exposure (`mayExpose`) is separate from execution (`mayExecute` through the Governor), progressive disclosure, refresh-without-polling, capability-loss flow (CAP-T05).
- **Phase 5 exit gate** — "a large synthetic catalogue stays prompt-efficient; missing tools degrade honestly". Both clauses are named in the doctrine.

## What was built

- **`+core/16-CAPABILITIES.md`** (new, doctrine): WHAT A CAPABILITY IS (stable vendor-neutral IDs, host names are aliases, registry is descriptive and executes nothing); THE ONE EFFECTS TAXONOMY (single vocabulary across registry/Governor/skills/audit, no parallel synonyms, conservative inference); DISCLOSURE IS NOT EXECUTION (mayExpose then mayExecute via Governor, one mapping, never bypassed); WHEN TO CONSULT THIS FILE (before planning tool-heavy work, the moment a needed tool seems missing, on mid-task capability loss); WHEN A CAPABILITY IS MISSING (the 48 §5 canonical three-line statement verbatim-shape + the forbidden list + DEGRADED/UNAVAILABLE → refresh once → search equivalent → re-plan or report with fallback; BLOCKED is a legitimate outcome); THE CATALOGUE STAYS LIGHT (progressive disclosure; a thousand-capability catalogue costs no more than a ten-capability one at session start).
- **`~START-HERE.md`** — step-6 routing row added verbatim from 48 §2: `` | `core/16-CAPABILITIES.md` | When a needed tool seems missing, or before planning tool-heavy work | ``.
- **`~runtime/payload/…`** — regenerated by `npm run sync:payload` (copies `core/`, `START-HERE.md`, rewrites install/build links; preserved authored `skills/` + `opencode/`). Payload now ships `core/16-CAPABILITIES.md` and the new START-HERE row.
- No runtime TS changes — doctrine + routing only, exactly the WP-049 shape (204aa94).

## Acceptance mapping

| Acceptance | Where |
|---|---|
| Packaging tests pass | `test/cli/payload.test.ts` runs inside `npm run verify` (7/7, incl. "payload is in sync with the doctrine source" + "every relative markdown link resolves"); verify exits 0 |
| Doctrine states the honest-unavailability wording of 48 §5 | `core/16-CAPABILITIES.md` "WHEN A CAPABILITY IS MISSING" — the three-line `UNAVAILABLE / Effect on the task / Fallback` form (with the "none — this part is BLOCKED" fallback clause) plus the verbatim forbidden list |
| 48 §2 routing | START-HERE.md step 6 contains the exact `core/16-CAPABILITIES.md` row; payload copy regenerated |

Also covered: doctrine names CAP-T05's rule (unavailable is named UNAVAILABLE, no fake call), the one-effects-taxonomy rule (CAP-T07), exposure≠execution (CAP-T04), registry-never-executes (CAP-T03), refresh-without-polling (CAP-T06), and the Phase 5 exit-gate both clauses.

## Surprises

- **`payload.test.ts` uses `node:test`, not vitest** — running it via `npx vitest run test/cli/payload.test.ts` reports "No test suite found" (the file has no `import.meta.vitest`/vitest suite), yet the suite still executes and passes (7 tests shown). The canonical gate is `npm run verify` (which runs `npm test` → the node:test runner), where the same suite passes and counts. Noted for WP-058 so the packaging gate is invoked through `npm test`, not vitest.
- Doc-21 terminology used verbatim (`mayExpose`, one effects taxonomy, `EXTERNAL_SIDE_EFFECT` conservative default) so the doctrine names mechanisms the WP-051/055 runtime actually built — no invented vocabulary.

## Files

- `+core/16-CAPABILITIES.md` (new, doctrine)
- `~START-HERE.md` (one routing row, 48 §2 wording)
- `~runtime/payload/core/16-CAPABILITIES.md`, `~runtime/payload/START-HERE.md` (regenerated by sync)
- `~.apex/upgrade/STATE.json` (WP-057 PENDING → DONE)

## Next

- WP-058 — capability invoke bridge + 3-tier disclosure (CRITICAL; after WP-054): CapabilityBridge search/describe/invoke through the Governor, EXT-T01/T07 entry loading, TLS-T08..T11, PERF-T09/T10, `apex_capability_search/describe/invoke` MCP tools.
