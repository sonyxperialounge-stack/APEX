# EVD-010
Packet: WP-016 (global home store)
When: 2026-09-10
Command: npm run verify
Cwd: D:/APEX/Army-V3/runtime
Exit: 0

Focused run: 8 tests / 2 suites, 0 fail (956ms), including BOOT-T02 with four real
concurrent Node processes racing ensure() on one absent home: one valid structure,
zero stray temp dirs, zero throws.

Full pipeline (literal tail):

```text
ℹ tests 760
ℹ suites 171
ℹ pass 760
ℹ fail 0
ℹ cancelled 0
ℹ skipped 0
ℹ todo 0
ℹ duration_ms 16709.2637
```

Done-when:
- two simultaneous first runs produce one valid structure and neither throws ✔ (4-process race)
- a read-only home yields READ_ONLY mode without creating anything ✔ (POSIX chmod; win32
  honourably asserts the VOLATILE contract instead — ACLs not a Node boundary, 30 §6)
- ensure() idempotent; future-schema marker (v99) NEVER downgraded — read-only + event ✔
- describe() re-probes live mode (a home ensured after open reports OK, not stale UNAVAILABLE) ✔
- subdir/lockFile shapes; kebab lock names; Doctor checks per 31 §5 (resolve, structure,
  schema, tempfiles, locks with WARN + remediation) ✔

Defects found and fixed in-packet (honest record):
1. My new-file license headers were a SHORT paraphrase of the canonical LONG header —
   HC-T03 has been strengthened to byte-identical comparison (startsWith canonical 17
   lines), which now mechanically catches paraphrases.
2. ensure() overwrote a future-schema home.json down to current (29 §7 violation) —
   now read-only + home.future_schema_readonly event.
3. new Date(now()) in a stores module tripped my own MOD-T03 scan — moved ISO formatting
   to core/ids.ts toIsoString so stores hold no Date-constructor token at all.
