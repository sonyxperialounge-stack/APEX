# EVD-063 — WP-063 Capability-first planning (24 §7, AUT-T03)

Date: 2026-09-11 · Packet: WP-063 (Dep WP-053 + WP-060) · Branch: upgrade/army-v4

## Context

24 §7: plans must be feasible against the actual registry. An absent
capability causes a re-plan, never a fake call. Done-when is AUT-T03.

## What was built

- `~runtime/src/engines/task-contract.ts` (+pure planning, no calls):
  `PlanStep` (id/title/requiredCapabilities/fallback),
  `collectRequiredCapabilities` (deduped, feeds the PERF-T10 disclosure
  seam), `checkPlanFeasibility(steps, isAvailable)` (per-step missing set;
  all-present → execute as planned; absent-with-fallback → replan onto the
  fallback with a 48 §5 UNAVAILABLE statement; absent-without-fallback →
  blocked with "none — this part is BLOCKED" and "Never issue the call").
- `~runtime/test/engines/task-contract.test.ts` (+4): all-present executes,
  absent-with-fallback replans (AUT-T03), absent-without-fallback blocks
  honestly, required-id collection dedupes.

## Verify

`npm run verify` → 1289 pass, 0 fail, 0 cancelled, exit 0 (~38s).
Was 1285 before (+4 new).

## Acceptance mapping

| Id | How |
|---|---|
| AUT-T03 | absent capability → replan-with-fallback or BLOCKED with UNAVAILABLE reasons; executable steps never name an absent id; no call is ever issued by the checker |

## Surprises

- A bad edit mid-packet deleted the `validateResumeCapsule` signature line;
  caught on read-back and restored with the new section inserted above it.
  Read the seam after every edit.

## Files

- `~runtime/src/engines/task-contract.ts`
- `~runtime/test/engines/task-contract.test.ts`
- `~.apex/upgrade/STATE.json` (WP-063 IN_PROGRESS → DONE)

## Next

WP-064 (failure taxonomy + recovery ladder, Dep WP-060).
