# EVD-066 — WP-066 Council triggers (26 §§8–9; FLT-T04)

Date: 2026-09-11 · Packet: WP-066 (Dep WP-065) · Branch: upgrade/army-v4

## Context

26 §8 convenes the council for high-impact or ambiguous calls; §9 keeps it
critique-only (no majority-vote truth). The packet adds migration-design,
skill-promotion-challenge and security-boundary triggers. Done-when is
FLT-T04.

## What was built

- `~runtime/src/engines/council.ts` (+three triggers, gate untouched):
  `ConveneReason` gains `migration_design` / `skill_promotion` /
  `security_boundary`; `ConveneContext` gains the matching optional flags;
  `shouldConvene` answers each with a naming reason. No status path added
  (CNC-006 stays green).
- `~runtime/test/engines/recall-council.test.ts` (+4): one test per new
  trigger asserting `yes` + exact trigger name; FLT-T04 runs two agreeing
  reviews and proves both finding sets stay unverified with zero
  verification records — consensus never becomes evidence.

## Verify

`npm run verify` → 1309 pass, 0 fail, 0 cancelled, exit 0 (~49s).
Was 1305 before (+4 new).

## Acceptance mapping

| Id | How |
|---|---|
| FLT-T04 | two agreeing reviews leave every finding `verified: false` and the ledger holds zero verification records |

## Surprises

- None. The existing `ctx()` helper spreads overrides, so the new optional
  flags needed no harness change.

## Files

- `~runtime/src/engines/council.ts`
- `~runtime/test/engines/recall-council.test.ts`
- `~.apex/upgrade/STATE.json` (WP-066 IN_PROGRESS → DONE)

## Next

WP-067 (completion states + Gate integration, Dep WP-061 + WP-060).
