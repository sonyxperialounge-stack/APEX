# EVD-065 — WP-065 Fleet delegation contracts (26 §§3–6; FLT-T01/T03/T05/T06)

Date: 2026-09-11 · Packet: WP-065 (Dep WP-064) · Branch: upgrade/army-v4

## Context

26 §3 gives every subagent a minimal contract (task, parent, objective,
allowed paths, prohibited effects, required capabilities, expected
evidence, write mode); §6 forbids concurrent edits of shared state without
coordination. Done-when is FLT-T01/T03/T05/T06.

## What was built

- `~runtime/src/engines/warden.ts` (+contracts, supervision untouched):
  `DELEGATION_WRITE_MODES` (NONE/READ_ONLY/SCOPED_WRITE/ISOLATED_WORKTREE/
  SERIALIZED_WRITE), `DelegationContract`, `validateDelegationContract`
  (scoped writers need explicit paths — never "do whatever is needed"),
  `delegationWrites`/`delegationOverlap`/`canRunConcurrently` (writers on
  overlapping paths serialize; readers never conflict), `canDelegateClose` +
  `canParentCloseChild` (child never closes parent; parent may close child;
  self allowed), `reconcileChildEvidence` (every accepted claim cites a
  passing verification the parent saw), `buildChildContext` (laws + task
  slice + required project context + one skill + usable capabilities; no
  personal-memory parameter exists, so unrelated memory cannot leak).
- `~runtime/test/engines/warden.test.ts` (+5): scope/mode validation,
  FLT-T01 close guard, FLT-T03 serialize-vs-concurrent, FLT-T05
  reconcile-vs-bare-vs-forged, FLT-T06 slice-only context.

## Verify

`npm run verify` → 1300 pass, 0 fail, 0 cancelled, exit 0 (~40s).
Was 1295 before (+5 new).

## Acceptance mapping

| Id | How |
|---|---|
| FLT-T01 | `canDelegateClose` refuses child→parent, allows self; `canParentCloseChild` allows parent→child only for its own children |
| FLT-T03 | overlapping SCOPED_WRITE pair → `concurrent: false` naming both tasks and paths; disjoint writers and overlapping readers → concurrent |
| FLT-T05 | cited passing evidence accepted; bare claim and forged id rejected with naming reasons; unresolved carried as open work |
| FLT-T06 | context contains task slice + caps, excludes planted unrelated memory by construction |

## Surprises

- None. The overlap check reuses the wave rule (`isUnder`) so packet waves
  and delegation contracts can never disagree on what "overlap" means.

## Files

- `~runtime/src/engines/warden.ts`
- `~runtime/test/engines/warden.test.ts`
- `~.apex/upgrade/STATE.json` (WP-065 IN_PROGRESS → DONE)

## Next

WP-065b (child limits: depth, budget, stall, cleanup; Dep WP-065; 54 §12).
