# EVD-068 — WP-068 Durable-state doctrine (48 §9; Phase-6 gate packet)

Date: 2026-09-11 · Packet: WP-068 (Dep WP-067) · Branch: upgrade/army-v4

## Context

48 §9 inserts the WHAT-SURVIVES-WHAT map after "The three levels" and the
48 §2 routing row for `core/14-DURABLE-STATE.md`. Done-when: packaging tests
pass and the map matches the implemented paths exactly.

## What was built

- `+core/14-DURABLE-STATE.md` (in the 15/16 voice): the survival table with
  exact implemented paths — project ledger files in `<project>/.apex/`,
  project memory `<project>/.apex/MEMORY.md`, handoff `<project>/.apex/
  HANDOFF.md`, personal facts `<global home>/memory/` (`records.jsonl`,
  `state.json`, `pending/mutations.jsonl`), skills `<global home>/skills/`
  (`<name>/SKILL.md`, `.bundles/`, `.archive/`, `pending/`), archive
  `<global home>/archive/` (`sessions.jsonl`, `events/`), trust `<global
  home>/trust/` (`skills.json`, `extensions.json`, `hooks.json`) — each
  verified against the store that owns it (global-home LAYOUT, memory-store,
  archive `archiveDirFor`, trust stores). The 48 §9 draft said `sessions/`;
  the code builds `archive/`, so the doc says `archive/` with the
  code-wins-and-the-doc-is-the-defect rule stated inside. Plus consult-when
  and honesty rules (read-only writes nothing, staged≠saved, remembered≠
  proven, degraded mode persists nothing).
- `~START-HERE.md` (one routing row, verbatim 48 §2): `core/14-DURABLE-
  STATE.md` before writing global memory or skills. The V2 rewrite stays in
  WP-070.
- Payload: `runtime/payload/core/14-DURABLE-STATE.md` + payload START-HERE
  regenerated by `sync:payload` (part of verify).

## Verify

`npm run verify` → 1313 pass, 0 fail, 0 cancelled, exit 0 (~45s).
Packaging tests (`test/cli/payload.test.ts`, `test/cli/packaging.test.ts`)
inside it green, including "payload is in sync with the doctrine source".

## Acceptance mapping

| Requirement | How |
|---|---|
| map matches paths exactly | every row checked against its owning module (see Context); the one draft-vs-code mismatch (`sessions/` vs `archive/`) resolved in favour of the code, honestly recorded |
| packaging green | full verify green with the new doctrine file shipped in the payload |

## Surprises

- The 48 §9 draft table names `<global home>/sessions/`; the shipped
  archive store resolves `<global home>/archive/` (WP-036 `archiveDirFor`).
  The doctrine follows the code and says so — the map is a report, not a
  second spec.

## Files

- `+core/14-DURABLE-STATE.md`
- `~START-HERE.md` (routing row)
- `~runtime/payload/core/14-DURABLE-STATE.md` + `~runtime/payload/START-HERE.md` (sync)
- `~.apex/upgrade/STATE.json` (WP-068 IN_PROGRESS → DONE; Phase 6 → 7A)

## Next

Phase 7A at WP-070 (START-HERE V2). Phase-6 gate below.

## Phase-6 exit gate

One governed loop, four golden scenarios: code → cascade + code-domain
verifier (WP-061 AUT-T01, cascade authoritative); research → sourced
research verifier (WP-061 AUT-T02, recall never passes); artifact →
physical deliverable validation (WP-062 AUT-T07, filename never proves
content); system operation → capability-first planning + Governor +
bounded recovery (WP-063 AUT-T03 replan-or-block, WP-064 ladder,
WP-065/065b delegation contracts + child limits). Handoff, completion
states and doctrine close the loop (WP-060 TASK-T01..T06, WP-066 FLT-T04,
WP-067 five states, WP-068 map). Suite: 1313 pass, 0 fail.
