# EVD-067 — WP-067 Completion states + Gate integration (24 §11, 45 §3.3)

Date: 2026-09-11 · Packet: WP-067 (Dep WP-061 + WP-060) · Branch: upgrade/army-v4

## Context

24 §11 fixes five task completion states; 45 §3.3 closes the vocabulary
(no synonyms). Subjective work takes COMPLETE_WITH_LIMITATION under
documented review criteria — never fabricated objective proof. The
Ledger still owns requirement truth; this names the task outcome.

## What was built

- `~runtime/src/engines/ledger.ts` (+pure evaluation, authority untouched):
  `TASK_COMPLETION_STATUSES` (exactly the five), `evaluateTaskCompletion`
  (blocker→BLOCKED; user choice→NEEDS_USER_DECISION; all verified→
  VERIFIED_COMPLETE; remainder + declared limitation→
  COMPLETE_WITH_LIMITATION naming the unverified ids; remainder without
  limitation→IN_PROGRESS with the remedy stated).
- `~runtime/src/mcp/tools.ts` (`apex_gate` contract only): description now
  teaches the five states and the subjective-proof rule. No handler change.
- `~runtime/test/engines/ledger.test.ts` (+4; see D-007): vocabulary,
  objective completion, subjective-never-objective (IN_PROGRESS then
  COMPLETE_WITH_LIMITATION with remaining), blocker/decision states.
- `~.apex/DECISIONS.md` (D-007): blast-radius note for the test-file touch.

## Verify

`npm run verify` → 1313 pass, 0 fail, 0 cancelled, exit 0 (~42s).
Was 1309 before (+4 new).

## Acceptance mapping

| Requirement | How |
|---|---|
| five states, limitation required | vocabulary test pins the five; IN_PROGRESS names the limitation remedy |
| subjective ≠ objective | no-verification input returns IN_PROGRESS (never VERIFIED_COMPLETE); with a declared limitation it returns COMPLETE_WITH_LIMITATION carrying the unverified ids |

## Surprises

- None. Ledger growth stays inside its +10% grandfathering.

## Files

- `~runtime/src/engines/ledger.ts`
- `~runtime/src/mcp/tools.ts`
- `~runtime/test/engines/ledger.test.ts` (D-007)
- `~.apex/DECISIONS.md` (D-007)
- `~.apex/upgrade/STATE.json` (WP-067 IN_PROGRESS → DONE)

## Next

WP-068 (durable-state doctrine, Dep WP-067) — the Phase-6 gate packet.
