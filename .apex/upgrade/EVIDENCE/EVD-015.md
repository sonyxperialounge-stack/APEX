# EVD-015
Packet: WP-021 (memory store read/commit)
When: 2026-09-10
Command: npm run verify
Cwd: D:/APEX/Army-V3/runtime
Exit: 0

Focused run: 15 tests / 6 suites, 0 fail — including:
- MEM-CON-T01: 20 REAL concurrent Node processes (spawned children, in-process CAS retry
  loops): all 20 records present exactly once, revision == 20 (2.4-2.7s wall)
- MEM-CON-T04: child SIGKILLed inside the commit critical section; canonical records.jsonl
  byte-identical after the kill; next writer recovers under the stale-lock policy
- CAS: stale writer rejected with MEMORY_REVISION_CONFLICT, loser never applied
- invalid/duplicate records refused before any write (revision unchanged)
- hot views render only active global records; retracted never renders; deleting both
  views and rebuilding loses nothing (C-008/MEM-T06 shape)

Full pipeline (literal tail):

```text
ℹ tests 804
ℹ suites 181
ℹ pass 804
ℹ fail 0
ℹ cancelled 0
ℹ skipped 0
ℹ todo 0
ℹ duration_ms 17160.0436
```

Notes: the commit contract is FULL-STATE with revision CAS (like committing an array):
callers read-modify-write; the store never merges (47 §4.7 — merging is WP-023/024's
librarian). My first stale-writer test modelled a patch-style writer and failed against
the real contract; corrected to read-modify-write, matching what the 20-process race
already did correctly.
