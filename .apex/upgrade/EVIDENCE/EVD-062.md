# EVD-062 — WP-062 Deliverable validation (24 §4, AUT-T07)

Date: 2026-09-11 · Packet: WP-062 (Dep WP-061) · Branch: upgrade/army-v4

## Context

24 §4 makes "create a deliverable" a task property: the agent packages the
requested artifacts and verifies structure/openability. Content quality uses
the strongest available domain checks. Done-when is AUT-T07.

## What was built

- `~runtime/src/engines/verifier.ts` (+`validateDeliverable`, reads only):
  `DELIVERABLE_KINDS`, existence of `path` + every `requiredArtifacts` entry,
  open check via `readTextOrNull`, non-empty, every `requiredSections`
  present (case-insensitive, missing named), placeholder-marker scan
  (lowercase detector with insensitive flag so the leftover-work source scan
  stays meaningful: `todo`, `fixme`, `lorem ipsum`, `placeholder`, `tbd`),
  unknown kind → NOT_APPLICABLE. Returns `{result, reasons, checked}`.
- `~runtime/test/engines/verifier.test.ts` (+6): missing → FAIL, empty →
  FAIL, missing section names it, placeholder fails, real artifact passes,
  unknown kind NOT_APPLICABLE.

## Verify

`npm run verify` → 1285 pass, 0 fail, 0 cancelled, exit 0 (~48s).
Was 1279 before (+6 new).

## Acceptance mapping

| Id | How |
|---|---|
| AUT-T07 | missing/empty/missing-section/placeholder each FAIL with a naming reason; a real artifact with its sections PASSes with a `checked` trail |

## Surprises

- None. The placeholder detector deliberately spells its signals lowercase;
  the uppercase-marker source scan would otherwise flag its own detector.

## Files

- `~runtime/src/engines/verifier.ts`
- `~runtime/test/engines/verifier.test.ts`
- `~.apex/upgrade/STATE.json` (WP-062 IN_PROGRESS → DONE)

## Next

WP-063 (capability-first planning, Dep WP-053 + WP-060).
