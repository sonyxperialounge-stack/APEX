# EVD-083 — WP-083: Migration fixture suite

## Packet
WP-083 — the full 33 §12 loop for every schema transition: forward, interrupted, idempotent-rerun, future-schema.
Dep: WP-018, WP-074. Files: `+test/migrations/migration-fixtures.test.ts` (5 tests).

## What was built
`test/migrations/migration-fixtures.test.ts` runs the complete 33 §12 loop against BOTH real migration surfaces — the transactional runner (`migrateStore`, 29 §5) and the production config migration (`Ledger.migrateConfigFile`, MIG-config-1-to-2) — plus Doctor's recovery path:

| Test | The 33 §12 step it carries |
|---|---|
| MIG-F01 forward | A realistic versionless V3 config fixture → `loadConfig()` → schemaVersion 2 stamped FIRST, every V3 key preserved in original order, values readable through the typed config; backup `config.json.apex.bak` holds the pre-migration bytes; journal carries one COMPLETED 1→2 entry naming the backup. |
| MIG-F02 idempotent re-run | Second `loadConfig()` on the migrated file: sha256 identical, zero new journal entries — already complete is a report, not a write. |
| MIG-F03 interrupted → recover | A STARTED journal entry with no terminal partner → `migrateStore` refuses (`MIGRATION_INTERRUPTED`, 29 §6); `findInterruptedMigrations` sees it; `runDoctor --repair` (areas: ["MIG"]) terminals it as FAILED — never deleted; the SAME migration then completes on the re-run. |
| MIG-F04 future-schema read-only | (a) runner refuses a v99 store (`READ_ONLY_FUTURE_SCHEMA`) before lock or read, bytes untouched; (b) the Ledger loads a v3 config read-only, notes it in `configIssues`, never downgrades/restamps; (c) Doctor with a future-schema home + an interrupted journal: repair refuses to act, the STARTED entry survives until the runtime is upgraded, and the check reports the read-only rule as the reason. |
| MIG-F05 legacy adoption + data integrity | Versionless legacy memory fixture adopted as v0 (29 §8), migrated 0→1 with record IDs byte-identical and each record gaining its migrated field; re-run reports `{migrated: false, fromVersion: 1, toVersion: 1, journalId: ""}` and moves no bytes. |

## Wiring fact documented by MIG-F03
The runner's journal defaults to store-adjacent `<stateDir>/migrations/`; Doctor's MIG check and repair read/write the HOME's `migrations/journal.json`. The test wires a store run to the home journal via `opts.journalDir` — the home journal is Doctor's recovery surface, and this is the participation pattern for any home-level store. (WP-087, Doctor completion, may widen the MIG check to also scan project `.apex/migrations/` where `migrateConfigFile` writes.)

## Mutation check (protocol §6 JUDGE)
Short-circuited the runner's interrupted-run detection (`if (interrupted)`) in `migration-registry.ts` → **MIG-F03 fails** (the crashed run is silently accepted). Restored byte-identical (`git diff` clean); suite back to 5/5.

## Verify (fresh, this packet)
Full `npm run verify` → **1411 pass / 0 fail** (was 1406; +5 MIG fixtures).

## Harness notes
`readJson<T>(file, fallback)` takes a REQUIRED fallback — journal/state reads in the suite pass explicit defaults.
