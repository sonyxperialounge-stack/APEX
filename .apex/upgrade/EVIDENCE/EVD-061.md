# EVD-061 — WP-061 Domain verifier registry (24 §3, 40 §10, 41 §16)

Date: 2026-09-11 · Packet: WP-061 (Dep WP-060) · Branch: upgrade/army-v4

## Context

24 §3 demands domain-appropriate evidence; 40 §10 keeps the code cascade
authoritative while adding a registry without weakening types; 41 §16 is the
`DomainVerifier` blueprint (`supports` + `verify` → PASS|FAIL|NOT_RUN|
NOT_APPLICABLE). 42 §4 forbids any write path in the verifier.

## What was built

- `~runtime/src/engines/verifier.ts` (+registry, no cascade change):
  `AUTONOMY_DOMAINS`, `DOMAIN_VERDICTS`, `DomainVerificationInput`
  (domain + evidenceIds + cascadePassed + sources), `DomainVerifier`,
  `createDomainVerifierRegistry` (no module singleton; duplicate ids refused;
  unknown domain → NOT_APPLICABLE; evidence-free PASS coerced to FAIL so no
  verifier manufactures truth), `codeDomainVerifier` (AUT-T01: cascade
  decides; PASS needs passing cascade AND cited evidence; missing cascade →
  NOT_RUN), `researchDomainVerifier` (AUT-T02: no cited primary source →
  FAIL; model recall never passes; single-source passes with a limitation),
  `registerAutonomyVerifiers`.
- `~runtime/test/engines/verifier.test.ts` (+7): AUT-T01 cascade semantics +
  no-manufacture, AUT-T02 sourced/unsourced/unlinked, unknown-domain
  NOT_APPLICABLE, liar-verifier coercion, duplicate-id refusal, no-write-path
  source assertion.

## Verify

`npm run verify` → 1279 pass, 0 fail, 0 cancelled, exit 0 (~44s).
Was 1272 before (+7 new).

## Acceptance mapping

| Id | How |
|---|---|
| AUT-T01 | code PASS only with `cascadePassed: true` + `V-` cited; FAIL on failed cascade; NOT_RUN with no cascade result |
| AUT-T02 | research with no `sources` → FAIL naming primary-source rule; sourced + cited → PASS; sourced uncited → FAIL |
| no manufactured PASS | liar verifier returning evidence-free PASS coerced to FAIL by the registry |
| 42 §4 | source test asserts no `writeFile`/`writeText`/`writeJson`/`unlink`/`rename`/`rm(` in verifier.ts |

## Surprises

- The no-write-path regex contains `rm\(` which also matches innocent words
  like `confirm(` — the new code avoids that whole word family.
- `npm run test:unit -- <file>` ignores its file arg (hard-coded globs);
  the gate stays the full `npm run verify`.

## Files

- `~runtime/src/engines/verifier.ts`
- `~runtime/test/engines/verifier.test.ts`
- `~.apex/upgrade/STATE.json` (WP-061 IN_PROGRESS → DONE)

## Next

WP-062 (deliverable validation, Dep WP-061).
