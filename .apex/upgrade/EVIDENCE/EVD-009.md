# EVD-009
Packet: WP-015b (sensitive-path read denylist)
When: 2026-09-10
Command: npm run verify
Cwd: D:/APEX/Army-V3/runtime
Exit: 0

Focused run: 9 tests / 2 suites, 0 fail. Full pipeline:

```text
ℹ tests 751
ℹ suites 169
ℹ pass 751
ℹ fail 0
ℹ cancelled 0
ℹ skipped 0
ℹ todo 0
ℹ duration_ms 17023.0947
```

Done-when (54 §11.2):
- SEC-T10: with default config, reading .ssh/id_rsa is refused by the GOVERNOR and the
  refusal names the rule ("protected-read" + builtin pattern) ✔
- SEC-T11: a glob override is rejected (READ_DENIED_SENSITIVE); an exact absolute-path
  override works, is audited in apex.log ("override used"), and does not unlock siblings ✔
- denylist UNIONED with user doNotRead (both still deny; plain files stay readable) ✔
- directory LISTING stays allowed: the check fires only on read-into-context decisions
  (isProtectedRead is called from the read rule of decide()) ✔
- no false positives on ordinary project files (README, src, package.json...) ✔

Note: the config key security.allowSensitiveRead arrives with WP-074 (44 §6 config
surface). Until then the Governor reads it defensively via an optional field — absent
means no overrides, which is the safe default. The strict-key config test is untouched.
