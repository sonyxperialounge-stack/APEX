# EVD-026
Packet: WP-031 (redaction chokepoint)
When: 2026-09-10
Command: npm run verify
Cwd: D:/APEX/Amy-V3/runtime
Exit: 0

Focused run (archive-store.test.ts): 6 tests / 1 suite, 0 fail (2 new WP-031 tests).

Full pipeline (literal tail):
```text
ℹ tests 871
ℹ suites 192
ℹ pass 871
ℹ fail 0
ℹ cancelled 0
ℹ skipped 0
ℹ todo 0
ℹ duration_ms 18895.9455
```

Done-when (`49` WP-031, `15 §5`, `15 §10` ARC-T04):
- A single `persistEvent` path sanitises (redacts) before writing; no caller may bypass it ✔
- `ARC-T04`: secret-bearing tool output is redacted before persist ✔
- Source scan proves exactly one `appendJsonl` call site inside `persistEvent`; `appendEvent` removed ✔

Changes:
- `runtime/src/stores/archive-store.ts`: renamed `appendEvent`→`persistEvent`;
  imports `redact` from `core/redact.ts`; redacts text via `redact(ev.text)` before
  building the record; sets `redactionApplied: true` unconditionally (removed caller override).
- `runtime/src/core/json.ts`: added `appendText` (non-JSONL text append for quarantine,
  42 §4: all writes through json.ts).
- `runtime/test/stores/archive-store.test.ts`: updated calls to `persistEvent`; added
  ARC-T04 test + source-scan test.
