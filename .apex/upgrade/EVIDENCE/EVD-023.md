# EVD-023
Packet: WP-029 (memory user surface)
When: 2026-09-10
Command: npm run verify
Cwd: D:/APEX/Army-V3/runtime
Exit: 0

Focused run: 7 tests / 1 suite, 0 fail — real spawned-CLI end-to-end against a temp
APEX_HOME (add -> list -> inspect with provenance; correct + retract leaves review
state; scanner-denied add refused nonzero; export redacted JSON; disable sets
memory.useGlobal=false in the PROJECT config with records intact; approve applies a
staged mutation after re-scan; unknown sub -> usage + nonzero).

Full pipeline (literal tail):

```text
ℹ tests 862
ℹ suites 190
ℹ pass 862
ℹ fail 0
ℹ cancelled 0
ℹ skipped 0
ℹ todo 0
ℹ duration_ms 20109.3143
```

Done-when: every operation in 10 §12 is reachable without editing internal files:
list/filter (list --kind/--status), inspect provenance (inspect <id>), add explicit
memory (add), correct/supersede (correct <id> <text>), retract (retract <id>),
approve/reject pending (approve|reject <id>), export (export --out), disable per
project (disable). MCP tools deferred to WP-072 per the packet note — the compact-index
budget they require does not exist until WP-053.

Notes (honest record): spawning the CLI as `node index.ts` silently drops flags/argv
in this sandbox — the working shape is the WP-012 pattern (--experimental-strip-types
--input-type=module -e "import; main()"). Tests also run the SOURCES, not bin/: the bin
prefers a possibly-stale dist/ mid-build; the packed artifact gets its own tests in
WP-082.
