# EVD-007
Packet: WP-014 (source-scan invariant extensions)
When: 2026-09-10
Command: npm run verify
Cwd: D:/APEX/Army-V3/runtime
Exit: 0

Focused sourcescan run (literal tail): 53 tests / 18 suites, 0 fail (266.0ms).

Full pipeline (literal tail):

```text
ℹ tests 723
ℹ suites 161
ℹ pass 723
ℹ fail 0
ℹ cancelled 0
ℹ skipped 0
ℹ todo 0
ℹ duration_ms 19016.2885
```

New scans, each with a planted-violation self-test that must PASS (i.e. the scan is
demonstrated to catch its violation):
- HC-T03 license header on every .ts (header + NOTICE markers; planted headerless file caught)
- C-018 no setInterval scheduler anywhere in src (planted catch)
- MOD-T03 no new Date()/Math.random() in src/stores/** and non-legacy engines (planted catch;
  legacy engine list = the 7 pre-upgrade engines, exempt by 47 §5 being a NEW-module rule)
- MOD-T02 import-graph cycle detection (DFS; planted a->b->c->a caught; real tree acyclic)
- MOD-T05 size budgets: new core files 400, stores 350, mcp/tools 1400; existing files
  grandfathered at current size (ledger.ts 1096, json.ts 599 — noted, warning-grade per 47 §3)
- 43 §8 terminology: playbook / long-term memory / LTM / scratch memory / temp memory /
  history DB / transcript store / permission level all absent; planted catch
- 45 §3 status-synonym closure: VERIFIED_OK/DONE_STATUS/etc absent; planted catch

Notes: three typecheck iterations were needed on the cycle test (TS narrows
closure-written variables to their initial type at the read point — `never` join errors);
final form reads the variable through an object wrapper TS cannot collapse.
