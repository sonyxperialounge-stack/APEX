# EVD-025
Packet: WP-030 (archive types and store)
When: 2026-09-10
Command: npm run verify
Cwd: D:/APEX/Army-V3/runtime
Exit: 0

Focused run (archive-store.test.ts): 4 tests / 1 suite, 0 fail.

Full pipeline (literal tail):
```text
ℹ tests 869
ℹ suites 192
ℹ pass 869
ℹ fail 0
ℹ cancelled 0
ℹ skipped 0
ℹ todo 0
ℹ duration_ms 20410.2963
```

Done-when (`49` WP-030, `54 §10` ARC spec):
- Sessions append/read/list round-trip with status transitions ✔
- Events append/read/list round-trip per session ✔
- Unknown event type refused with named error (ARCHIVE_EVENT_MALFORMED) ✔
- Malformed events quarantine to `events/quarantine.jsonl` and reads survive ✔

Decisions:
- ArchiveEvent + SessionRecordV1 types added to `src/core/types.ts` per `15 §3` + `28 §6`,
  using `as const` arrays (no `enum`, per `42 §2`).
- `appendSession` input makes `id` and `taskIds` optional via `Partial<Pick<>>` (same pattern
  as `appendEvent`); the implementation generates defaults.
- `readJsonlSafe` writes quarantined lines to a per-file `.quarantine` side file; the archive
  store additionally redirects them to the shared `events/quarantine.jsonl` (domain contract
  per `15 §3`) via a new `appendText` in `core/json.ts` (42 §4: all writes through json.ts).
- No `new Date(`/`Math.random(` in store: `now` is injected, `newId`/`toIsoString` from
  `core/ids.ts`.
